/*
write an arrow function named "calculateTotal" that tales two parameters: "price" and "quantity".
The function should return the total cost by multiplying the "price" and "quantitty"
store the result in a vari naed "totalCost" 
*/ 

const calculateTotal =(price, quantity)=>  price * quantity
    


let totalCost = calculateTotal(499 , 100)
console.log(totalCost)