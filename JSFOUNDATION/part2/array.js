let heros = ['superman', 'batman', 'spiderman',1, true, null, undefined, Symbol('hitesh')];
console.log(heros); //[ 'superman', 'batman', 'spiderman' ]
console.log(typeof heros); //object 


const username = {
    firstName: "yuvi",
    lastName: "Paliwal",
    isLoggedin: true
};

let anotherUser = ["yogesh" , "Paliwal", true];
console.log(anotherUser[0]); //yogesh